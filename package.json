{
  "name": "pathlab-connect",
  "version": "1.0.0",
  "scripts": {
    "dev": "netlify dev",
    "build": "vite build",
    "postinstall": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  }
}
